import SwapBox from "@/features/home/SwapBox";

const Home = () => {
  return (
    <section className="flex items-center justify-center md:h-[80vh] h-full md:pb-0 pb-3">
      <SwapBox />
    </section>
  );
};

export default Home;
