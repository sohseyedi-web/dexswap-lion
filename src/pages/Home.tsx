import SwapBox from "@/features/home/SwapBox";

const Home = () => {
  return (
    <section className="flex items-center justify-center h-[80vh] py-8">
      <SwapBox />
    </section>
  );
};

export default Home;
